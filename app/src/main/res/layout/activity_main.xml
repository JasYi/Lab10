<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/results"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="get API info"
        app:layout_constraintTop_toBottomOf="@+id/results"
        app:layout_constraintLeft_toLeftOf="@id/results"
        app:layout_constraintRight_toRightOf="@+id/results"
        android:id="@+id/getBtn"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="post API info"
        app:layout_constraintTop_toBottomOf="@+id/getBtn"
        app:layout_constraintLeft_toLeftOf="@+id/getBtn"
        app:layout_constraintRight_toRightOf="@+id/getBtn"
        android:id="@+id/postBtn"/>

</androidx.constraintlayout.widget.ConstraintLayout>